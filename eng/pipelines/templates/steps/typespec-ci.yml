parameters:
- name: Folder
  type: string
- name: DisplayName
  type: string

steps:
- script: |
    npm install
    ts-node src/TypeSpecValidation.ts ${{parameters.Folder}}
  displayName: ${{ parameters.DisplayName }}
  workingDirectory: $(Build.SourcesDirectory)/eng/tools/TypeSpecValidation/
  condition: succeededOrFailed()
