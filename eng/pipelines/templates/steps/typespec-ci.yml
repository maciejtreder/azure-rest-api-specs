parameters:
- name: Folders
  type: object

steps:
- ${{ each folder in parameters.Folders }}:
  - script: ts-node eng/tools/TypeSpecValidation/src/TypeSpecValidation.ts ${{folder}}
    displayName: split($folder, '/')[-1]
    condition: succeededOrFailed()
