parameters:
- name: Folder
  type: string
- name: DisplayName
  type: string

steps:
- script: ts-node eng/tools/TypeSpecValidation/src/TypeSpecValidation.ts ${{parameters.Folder}}
  displayName: ${{ DisplayName }}
  condition: succeededOrFailed()
